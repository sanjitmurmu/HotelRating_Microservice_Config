spring:
 application:
  name: Config-Prod

eureka:
 instance:
  prefer-ip-address: true
 client:
  register-with-eureka: true
  fetchRegistry: true
  service-url:
   defaultZone: http://localhost:8761/eureka
